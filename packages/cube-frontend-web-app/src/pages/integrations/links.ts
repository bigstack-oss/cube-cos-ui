import { CosRoutesEnum } from '@cube-frontend/web-app/enum/routes'

export const links = {
  application: CosRoutesEnum.INTEGRATIONS_APPLICATIONS_PAGE,
  storage: CosRoutesEnum.INTEGRATIONS_STORAGES_PAGE,
}
